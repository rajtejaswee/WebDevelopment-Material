const num = 1234;

if (num === 1234) {
    const greeting = "Hello World";
}

// const num is accessible in if block whereas greeting isn't accessible outside the block, same is the condition is applied to let but if we use var we can access it outside the if block.